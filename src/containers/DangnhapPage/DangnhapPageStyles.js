const dnPageStyles = theme => ({
    bckG: {
        backgroundColor: theme.palette.primary.main,
        display: 'flex',
        alignItems: 'center',
        justifyContent: 'center',
        textAlign: 'center',
        minHeight: '100vh',
    },
    textField: {
        width: '100%',
    },
})

export default dnPageStyles